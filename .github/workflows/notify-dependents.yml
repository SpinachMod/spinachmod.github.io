name: Notify Dependents

on:
  push:
    branches:
      - develop
  workflow_dispatch:
    inputs: {}

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger desktop update
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.AUTOMATION }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/SpinachMod/desktop/dispatches \
            -d '{"event_type":"upstream-updated","client_payload":{"source":"scratch-gui"}}'
